# Harbormasterd Runtime Dependencies
# (Dev/test deps live in pyproject.toml under [project.optional-dependencies].)

# Core web framework and API
fastapi>=0.104.0
pydantic>=2.0.0
uvicorn[standard]>=0.24.0
requests>=2.31.0

# CLI and user interface
click>=8.1.0
rich>=13.6.0
pyyaml>=6.0.1
keyring>=24.2.0

# System monitoring and process management
psutil>=5.9.0

# TLS / crypto
cryptography>=41.0.0

# Note: sqlite3 ships with CPython — do not pip-install it.
